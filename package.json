{
  "name": "root",
  "private": true,
  "workspaces": [
    "dashboard"
  ],
  "scripts": {
    "dev": "npm run dev --prefix dashboard",
    "build": "npm install --include=dev --prefix dashboard && npm run build --prefix dashboard",
    "preview": "npm run preview --prefix dashboard"
  }
}
