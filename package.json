{
  "name": "root",
  "private": true,
  "workspaces": [
    "dashboard"
  ],
  "scripts": {
    "dev": "npm run dev --prefix dashboard",
    "build": "npm install --include=dev --prefix dashboard && npm run build --prefix dashboard && node -e \"const fs = require('fs'); fs.cpSync('dashboard/dist', 'dist', { recursive: true });\"",
    "preview": "npm run preview --prefix dashboard"
  }
}
